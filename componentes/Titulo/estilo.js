import { StyleSheet } from "react-native";

const estilo = StyleSheet.create({
  box: {
    backgroundColor: '#000',
    alignItems: 'center',
    padding: 10,
  },
  texto: {
    fontSize: 28,
    color: '#61dafb',
  }
});

export default estilo;