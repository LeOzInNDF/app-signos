import { StyleSheet } from "react-native";

const estilo = StyleSheet.create({
  boxSigno: {
    padding: 10,
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 2,
    marginBottom: 10
  },
  nomeSigno: {
    fontSize: 18,
    marginBottom: 5
  }
});

export  default estilo;